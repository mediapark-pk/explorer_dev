import { makeStyles, Theme } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme: Theme) => ({
    root: {
        alignSelf: 'center',
        flexBasis: '16.05%!important',
        flexGrow: 1,
        flexShrink: 1,
        marginLeft: '2.47%',
        marginRight: '0%',
        minHeight: '67px',
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'flex-start',
        backgroundColor: 'rgba(0, 0, 0, 0)'
    }
}));
