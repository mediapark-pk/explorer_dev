import { makeStyles, Theme } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme: Theme) => ({
    root: {
        flexBasis: '54.17%!important',
        flexGrow: 1,
        flexShrink: 1,
        marginLeft: '1.06%',
        marginRight: '0%',
        minHeight: '293px',
        backgroundColor: 'rgba(0, 0, 0, 0)'
    }
}));
